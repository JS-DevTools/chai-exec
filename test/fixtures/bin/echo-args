#!/usr/bin/env node

/**
 *
 * This program simply echoes its arguments to stdout.
 *
 */
"use strict";

let args = process.argv.slice(2);

for (let [index, arg] of args.entries()) {
  console.log(`Argument #${index + 1}: ${arg}`);
}
